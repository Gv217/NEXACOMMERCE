import React from "react";
import ReactDOM from "react-dom/client";
import NexaCommerce from "./NexaCommerce";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <NexaCommerce />
  </React.StrictMode>
);